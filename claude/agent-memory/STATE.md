# Agent Memory — Global State

Standing facts only. Not a session journal. Keep this lean — it loads every session.
Update via the `/retro` skill — this file is a static `@import`, never auto-written.

## Verified Facts
- QPay old-core backends (Stack A — most qpay-* services): Express 5 + Babel (NOT TypeScript) + qpay-sequelize-postgres + Bull + Joi + Sentry. New-core is Fastify 5 + TypeScript + BullMQ + Zod (Stack C) — full three-stack map in ~/.claude/qpay-context/backend.md
- QPay frontend Gen1: Next.js 12 + React 17 + TypeScript + Ant Design 4 + Redux + SWR + Formik
- QPay frontend Gen2: Next.js + Tailwind + Zustand (e.g. qpay-fi-admin-web)
- Backend lint: eslint-airbnb + eslint-plugin-prettier — NO standalone .prettierrc; run eslint --fix only
- Frontend lint Gen 1/1.5/2: standalone .prettierrc — prettier first, then eslint --fix. Gen 3: eslint flat config only (`eslint.config.mjs`), no .prettierrc
- Backend qpay-micro-logging replaces console.log — always use it in backend service code
- No test framework installed in most backend services (qpay-customer-service uses babel-node for tests)
- QPay repos: old-core (git.qpay.mn) at /Users/dev/QPay/ · new-core (github.com/qpay-mn) at ~/qpay-mn/
- QPay workspace handoff authority is /Users/dev/QPay/AGENT_GUIDE.md (referenced by QPay/AGENTS.md)
- Codex mirrors Claude: ~/.codex/{agents/*.toml, skills/, rules/}. Claude files (~/.claude/agents/*.md) are source of truth; Codex .toml are generated ports — prompt-defense baseline + Claude body verbatim + `## Codex Compatibility` footer, preserving each toml's effort/sandbox/nicknames (regen: agent-setup/scripts/resync-codex-agents.py — roster is HARDCODED in its `AGENTS` list + `NEW_AGENT_DEFAULTS`; add new agents there or they're silently skipped. Needs python ≥3.11 for tomllib: use /opt/homebrew/bin/python3.12, system python3 is 3.9). Codex also has execution specialists with no Claude equivalent — live roster in QPay/AGENT_GUIDE.md. Both tools have their own native reviewer agents — Claude need not review Codex's changes or vice versa.
- Canonical agent-config repo = /Users/dev/GIthub/agent-setup (private GH Ganzorig2022/agent-setup). agent-stack and agent-system-starter are superseded — do not treat them as live. After config changes: sync live files → repo → commit.
- ~/.codex/rules/qpay/{backend,frontend}.md are byte-identical copies of ~/.claude/qpay-context/ — edit the Claude file only, then cp to Codex; `diff` is the drift check.
- Executors are usually Codex and OpenCode (NOT Claude). Default flow: Claude plans/reviews/orchestrates, then hands implementation off to Codex or OpenCode via the handoff-impl skill / AGENT_GUIDE.md. Prefer a self-contained handoff brief over implementing large changes directly. In-session delegation: `codex` Claude Code plugin (`claude plugin marketplace add openai/codex-plugin-cc` then `claude plugin install codex@openai-codex` — plugin name is `codex`, NOT the repo name) gives /codex:rescue|review|transfer; headless equivalent `codex exec --sandbox workspace-write -C <repo> "<brief>"`. Proven loop (2026-07-04): brief → Codex implements → matching Claude reviewer agent on the diff → orchestrator applies fixes → commit; the reviewer step is not optional — it caught a CRITICAL the executor missed.
- `gh-axi` is the standard GitHub tool for ALL agents (Claude, Codex, OpenCode) — not just a `gh` substitute. Use it for issues, PRs, comments, workflow/CI runs, and releases; prefer compact structured output and targeted queries over full logs/JSON dumps.
- Live phone mirror for Claude Code sessions (Claude mobile app unavailable in user's country): run `claude` inside tmux on the Mac (`tmux new -s <name>`; `~/.tmux.conf` sets `window-size latest`), phone attaches from Termux via `ssh mac` (phone's `~/.ssh/config`: Host mac → dev@100.89.218.100 tailnet IP, `RemoteCommand tmux new -As claude`, key auth). Needs Tailscale connected on both ends + Mac awake (`caffeinate -s`).
- QPay old-core git host `git.qpay.mn` (GitLab) is only reachable on the corporate VPN (FortiClient) — off-VPN, DNS won't resolve, so clone/fetch/push to old-core repos fail. New-core (`github.com/qpay-mn`) is public/normal.
- X content strategy = deepdive (2×/month, human-posted) + tech-brief reply engine. The daily x-draft-factory + x-post auto-posting pair is DECOMMISSIONED (launchctl disabled + `~/Desktop/x-drafts/.no-auto-post` kill switch) — short-form original posts don't pay at a small follower count; revisit when the audience justifies it. Scripts/plists kept for re-enable.
- kb-lint = weekly (Sun 10:00, launchd com.dev.kb-lint) read-only audit of always-loaded memory (STATE.md, CLAUDE.md, qpay-context): mechanical checks (dead path refs, Claude↔Codex mirror drift, dated claims >45d) + one headless-claude semantic pass (contradictions/staleness). Report → ~/kb-lint/<date>.md + Telegram. Fixes stay manual (/retro or direct edit); script at ~/.local/bin/kb-lint.py.
- /Users/dev/GIthub/ecc = local clone of everything-claude-code (MIT, ~67 agents / 270+ skills — counts drift, it's actively updated) — approved porting source for new agents/skills. Porting convention: reviewer-type agents get sonnet + read-only tools (Read/Grep/Glob/Bash — report only, executors fix), and the per-agent Prompt Defense block is stripped (covered by CLAUDE.md @import).
- Reviewer-agent evals: agent-setup/evals/reviewers/ (6 seeded-bug tasks, baseline 100% recall, committed) + ~/.local/bin/agent-evals.py. Manual-only, no cron. After editing a reviewer prompt: `agent-evals.py --agent <name>` (1–3 sonnet calls). Regression = exit 1 + Telegram. Task schema + calibration rules in its README; grader lesson: mention ≠ flag — audit saved raw outputs before trusting scores. Alarm self-test: `agent-evals.py --drill <agent>` (gutted inline agent; refuses --save-baseline).
- Canonical scheduled-jobs reference = agent-setup/docs/scheduled-jobs.md. House rule: generate always, deliver eventually — network stages gated with long retries (material + date captured at fire time); failed Telegram sends queue to ~/.outbox/, flushed every 30 min by com.dev.outbox-flush.
- Per-subagent usage (model + tokens): Claude = `subagentStatusLine` setting (live in agents panel) + SubagentStop hook → ~/.claude/hooks/subagent-usage.log; Codex = notify wrapper → ~/.codex/log/subagent-usage.log.
- ThinkPad T470 (Fedora 44, 192.168.1.39) revived 2026-07 as homelab server: Docker stack in /opt/homelab/docker-compose.yml (traefik, portainer, vaultwarden, nextcloud+mariadb, uptime-kuma, jellyfin, wireguard — auto-starts via restart policy), all containers on one bridge network `homelab_homelab` (reachable from each other by container name). agent-setup cloned at ~/agent-setup there; live ~/.claude files come from it (STATE.md is a symlink into the clone — edits land in the repo working copy, commit after). Claude's Bash tool on this box always silently denies `sudo` (no prompt shown, confirmed across sessions) — hand exact privileged commands to the user via the `!` prefix instead of retrying. Backups live in /opt/homelab/backup/ (dump-apps.sh, btrfs-snap.sh, backup-health.sh, lib-backup.sh, config in backup.env incl. KUMA_PUSH_URL) with systemd units in backup/systemd/ that are NOT auto-installed — need a one-time sudo cp into /etc/systemd/system + daemon-reload + enable --now. Vaultwarden is also reachable at https://homelab.tailfe4036.ts.net (tailscale serve, tailnet-only). Also used interactively as a GNOME (Wayland) desktop, not just a headless server. Default terminal = Ghostty (GPU-accel, config ~/.config/ghostty/config); Ptyxis removed; tmux is on-demand only — the ~/.local/bin/tmux-workspace auto-attach is deliberately NOT wired into Ghostty (it force-joins one shared `workspace` session and mirrors tabs, so /exit in one kills all). GNOME gotcha: gsd-color can run away pegging a full core (~80%, warms chassis) — fix is `systemctl --user mask org.gnome.SettingsDaemon.Color.service` (masked here; trade-off = Night Light off); first move for "box slow/hot" = `top -b -n2 -d5` to spot a pegged-and-idle process. Fingerprint service python3-validity disabled (Validity sensor flaky on Linux, errored every login). backup-health KUMA push now points direct at http://127.0.0.1:3001/api/push/<token> (bypasses traefik) in backup.env. Touchpad forced to native SMBus/RMI4 via kernel arg `psmouse.synaptics_intertouch=1` (grubby --update-kernel=ALL, persistent) — default was slow PS/2 mode (`SynPS/2`) making 2/3-finger gestures choppy; `psmouse` is builtin so boot-only (no runtime reload), device now enumerates as `Synaptics TM3276-004` on rmi4-00/i2c-6. Backout if suspend/resume freezes pointer: `grubby --remove-args` + reboot.

## Lessons Learned
- `~/.claude/rules/*.md` auto-loads into EVERY session globally; `paths:` frontmatter is ignored at user level (CC bugs #16299/#21858). Keep project-specific context in project CLAUDE.md imports, not user-level rules.
- CLAUDE.md + always-on rules are the main per-turn token cost — keep them lean; push detail into skills/agents (lazy-loaded).
- Custom subagents load ~/.claude/CLAUDE.md and its `@imports` (the built-in Explore/Plan agents skip it). Share content into all agents via a file `@import`'d from CLAUDE.md, not per-agent copies — e.g. the deduped Prompt Defense baseline now lives once at ~/.claude/prompt-defense.md.
- STATE.md is a static `@import`, NOT Claude Code's subagent persistent-memory feature (that needs `memory: user|project|local` frontmatter and lives at ~/.claude/agent-memory/<agent>/MEMORY.md). Nothing auto-writes STATE.md — capture learnings with `/retro`.
- Agent `model:` frontmatter IS honored: `sonnet|opus|haiku|fable|inherit|<full-id>` (defaults to `inherit`).
- settings.json hook commands run via shell — write `$HOME/...`, never a hardcoded home path. Hardcoded /Users/dev silently disabled ALL hooks on the Fedora box (no error; hooks just never fire). Hooks load at session start — path fixes need a new session.
- Hook mechanics: output channels — top-level `systemMessage` = user-facing non-blocking; `hookSpecificOutput.additionalContext` = injected for Claude's next request; `decision:"block"`+`reason` (or exit 2) = blocks & feeds Claude. Stop fires every turn-end (no true session-end event) — throttle via marker file; `stop_hook_active` guards recursion. PostToolUse can match a specific tool name (`ExitPlanMode` = fires on plan approval — the "plan is done" interception point); hooks CANNOT add choices to the native plan-approval menu.
- Headless `claude -p` (and `--agent`) inherits `permissions.defaultMode` — with global "plan", agents write findings to a plan FILE and stdout gets only a handoff line (looks like the agent found nothing). ALWAYS pass `--permission-mode default` in automation; also retry transient "API Error: ConnectionRefused" outputs in long sequential runs.
- SubagentStop payload: use `agent_transcript_path` (`transcript_path` = the MAIN session — docs are wrong); the hook can fire before the agent transcript flushes (poll ~3s). `subagentStatusLine` stdin = `{tasks:[{id=agentId, description, tokenCount(live), status}]}` per refresh, output `{"id","content"}` JSON lines; model NOT in payload — read it from the agent transcript.
- X/Twitter with no API token (verified 2026-06): public Nitter is dead (`nitter.net` 200-but-empty, others 403), RSSHub `/twitter` is 404. Only token-free route to real X posts is a logged-in headless browser. Bluesky RSS (`bsky.app/profile/<handle>/rss`) still works as a reliable proxy source. X's hosted MCP (api.x.com/mcp, June 2026) evaluated and SKIPPED: read-only (no posting) and bills X API pay-per-use (~$0.005/post read ≈ $37/mo for the daily harvest, no free tier) — fallback only if headless scraping dies permanently.
- `chrome-devtools-axi` for unattended/cron browser scraping: `CHROME_DEVTOOLS_AXI_USER_DATA_DIR` gives a persistent profile (login survives runs/reboots), headless KEEPS the logged-in session, and the bridge persists across `npx -y chrome-devtools-axi` calls. The X session can still be revoked account-side (it's a borrowed account — owner logins/password changes kill it); symptom = login walls in every X job; fix = `python3 ~/.local/bin/x-harvest.py --login` (headed window, sign in, close). Under launchd's sparse PATH, resolve npx by absolute path (glob newest `~/.nvm/versions/node/*/bin/npx`) — shell PATH isn't present. Working pattern in `~/.local/bin/x-harvest.py`.
- Automating X.com **writes** via `chrome-devtools-axi` (headless): posting a single tweet + editing profile fields works, but multi-tweet **threads** are unreliable — `/compose/post` renders duplicate composers (scope selectors to `[role="dialog"]`) and X auto-restores stale drafts — replace them with selectAll (only when text is present) + `insertText` in one event; NEVER `execCommand('delete')` on the composer, it desyncs Draft.js (text lands visually but React registers nothing, so the send button stays disabled). Set the editor via `execCommand('insertText')`, not `.value`; use REAL CDP `click @uid` for add/menu/pin buttons (synthetic `.click()` is ignored, though it works for some like profile Save). Headless stays logged in; headed can come up logged-**out** on the same profile. Since axi ≥0.1.26, snapshot lines may append attribute flags after the label (`button "Reply" disableable disabled`) — match label + `\bdisabled\b` as a word, never anchor to line-end. After `insertText`, React/Draft.js re-renders the inline Reply/send button to *enabled* a beat later and the a11y snapshot lags the DOM — snapshotting for the send-button uid immediately returns None (`Reply button not found/clickable`, 0/N posted). Poll the button's DOM `aria-disabled` (`[data-testid="tweetButtonInline"]`) until enabled BEFORE snapshotting the uid; never snapshot straight after setting text.
- Telegram Bot API = free unattended push channel for local automations: @BotFather bot creation is free (no billing/card); the user must message the bot ONCE before it can DM them (grab `chat_id` from `getUpdates`); `token`+`chat_id` in env; `sendMessage` caps at 4096 chars so chunk long messages.
- The auto-mode classifier blocks Claude from ANY edit to `.claude/settings*.json` (Self-Modification rule — not just permissions changes), from `mv`/`rm` of pre-existing repos, and from installing plugins from external repos — even with in-chat user approval. Don't retry; hand the exact command to the user to run via the `!` prefix.
- User's daily terminal is cmux (com.cmuxterm.app) — a plain terminal emulator, so tmux runs fine inside its panes; only sessions needing phone-mirror must live in tmux. Phone is a Xiaomi (MIUI) — exclude Tailscale from battery optimization or it gets killed in background.
- launchd python jobs CANNOT read or write `~/Desktop` subtrees created from a terminal context — per-file `com.apple.macl` TCC ACLs make globs silently return empty and writes fail `Errno 1` — and the failure is silent enough to blind a script's own idempotency checks. Keep automation output in `~/<dir>` and symlink onto Desktop for visibility. Related: lid closed with `SleepDisabled=1` keeps the Mac running but Wi-Fi is often down at launchd fire time; calendar jobs are never lost to sleep (they fire once at next wake, lost only on shutdown) — gate network stages with long retries, don't trust the scheduled slot.
- Two browser tools, different anchors: **Claude-in-Chrome MCP** acts inside the Chrome profile bound to the *session's* account — can be a different Google login than the user's own profile. **computer-use** screenshots the physical display but grants browsers **read tier** (screenshot-only; click/type/scroll blocked) — use the Chrome MCP to interact. Multi-monitor: a window may render split; `switch_display auto` + re-screenshot recovers the main monitor.
- btrfs read-only snapshots (`btrfs subvolume snapshot -r`) inherit the SOURCE subvolume's top-dir mtime, not a creation timestamp — a freshness check using `stat`/`find -printf %T@` on the snapshot's top dir reads wildly stale ages whenever the source's top dir rarely changes (e.g. `/home`). Fix: read the real creation time via `btrfs subvolume show <path>` → `Creation time` field, not mtime. `stat`'s btrfs birth-time (`%W`) is not a reliable substitute either — it returned an identical bogus timestamp for source and snapshot in testing.
- `tailscale serve`'s HTTPS listener is SNI-routed: connecting by the raw tailnet IP gives `TLS alert internal error`; only the MagicDNS hostname (e.g. `homelab.tailXXXX.ts.net`) resolves through tailscaled's TLS terminator to the right serve config. Always target monitors/scripts at the hostname, never the IP.
- CodexBar (AI usage-limit tracker, github.com/steipete/CodexBar) prebuilt Linux **glibc** release segfaults on the homelab Fedora 44 box (`libcurl.so.4: no version information available` warning, then SIGSEGV) — the static **musl** build (`linux-musl-x86_64.tar.gz`) runs fine. Installed at `~/.local/bin/codexbar` + a `codexbar-compact` wrapper (jq one-liner: `Codex 44%/53%  Claude 4%/16%`) feeding GNOME's Executor extension to mimic the macOS menu bar. `codexbar usage` takes ~15-20s per call (network) — any poller needs a ≥60s interval.
- GNOME Shell extensions that spawn commands (e.g. Executor) run them with a minimal environment — `~/.local/bin` is NOT on `PATH` — so any script they call must use absolute paths for every binary it invokes, not rely on the interactive shell's PATH.
