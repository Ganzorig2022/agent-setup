# Codex portable preferences — seed template.
#
# install.sh copies this to ~/.codex/config.toml ONLY if that file does not exist
# (Codex rewrites config.toml constantly with machine-specific project trust + paths,
# so it is never symlinked or overwritten). Merge new prefs by hand if needed.
#
# Intentionally EXCLUDED (machine-specific, do not sync):
#   notify, [projects.*], [marketplaces.*], [mcp_servers.*], [plugins.*],
#   [desktop.*], [tui.model_availability_nux], [[skills.config]]

model = "gpt-5.5"
model_reasoning_effort = "high"
plan_mode_reasoning_effort = "high"
web_search = "cached"
personality = "pragmatic"
approvals_reviewer = "user"
suppress_unstable_features_warning = true

[agents]
max_threads = 8
max_depth = 1
job_max_runtime_seconds = 1800

[features]
collaboration_modes = true
prevent_idle_sleep = true
goals = true
terminal_resize_reflow = true
js_repl = false

[notice]
fast_default_opt_out = true

[tui]
status_line = ["model-with-reasoning", "context-remaining", "five-hour-limit", "weekly-limit", "current-dir", "codex-version"]
status_line_use_colors = true
pet = "disabled"
